#include "main.h"
#include <stdlib.h>
#include <stdio.h>

char *argstostr(int ac, char **av)
{
    int i;
    if (ac == 0 || av == NULL)
    {
        return NULL;
    }
    for (i = 0; i < ac; i++)
    {
        
    }
}